---
title: Installation
description: Install the ACT CLI and set up your development environment.
---

## Install act-cli

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs>
<TabItem label="npm">
```bash
npm install -g @actcore/act-cli
```
</TabItem>
<TabItem label="pip">
```bash
pip install actcore
```
</TabItem>
<TabItem label="Cargo">
```bash
cargo install act-cli
```
</TabItem>
</Tabs>

## Verify Installation

```bash
act --version
```

## Rust Toolchain Setup

ACT components target WebAssembly. You need the nightly Rust toolchain with the `wasm32-wasip2` target:

```bash
rustup toolchain install nightly
rustup target add wasm32-wasip2 --toolchain nightly
```

:::tip
Use a `rust-toolchain.toml` in your project root to pin the toolchain:
```toml
[toolchain]
channel = "nightly"
targets = ["wasm32-wasip2"]
```
:::

## Scaffold a Component

```bash
# Using the copier template
pip install copier
copier copy gh:actcore/act-component-template my-component
```

Or manually — create a new Rust library project:

```bash
cargo new --lib my-component
cd my-component
```

Add dependencies to `Cargo.toml`:

```toml
[lib]
crate-type = ["cdylib"]

[dependencies]
act-sdk = "0.2"
```

## Next Steps

- [Create your first component](/docs/getting-started/first-component/)
